#include <stdio.h>
int main(){
    short alpha1;
    short alpha2;
    short alpha3;
    short alpha4;
    short alpha5;
    scanf("%c %c %c %c %c", &alpha1, &alpha2, &alpha3, &alpha4, &alpha5);
    printf("%c\n%c\n%c\n%c\n%c", alpha1+1, alpha2, alpha3+1, alpha4, alpha5+1);
    return 0;
}
